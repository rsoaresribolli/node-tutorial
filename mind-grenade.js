const number1 = 5;
const number2 = 10;

function addValures() {
    console.log(`The sum of ${number1} and ${number2} is ${number1 + number2}`);
}

// Function inside module is ran without needind to call it
addValures();
